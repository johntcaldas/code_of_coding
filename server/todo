Logging
figure out success = false cases; started work on sensors
feature idea: ifconfig
do the name="disk name" value="sda" way of passing data to the browser.
decide on how, and then comment tops of javascript files.
